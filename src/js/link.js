export class link {
    constructor(link) {
        this.link = link
    }

    async setLink() {
        const URL = ''
        const response = await fetch(URL)
        const data = await response.json()

    }

    async getLink() {
        const URL = ''
        const response = await fetch(URL)
        const data = await response.json()

    }

}